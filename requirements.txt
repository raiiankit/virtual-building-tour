# Core (required)
fastapi>=0.110
uvicorn[standard]>=0.27
sqlalchemy>=2.0
pydantic>=2.5
python-multipart>=0.0.9        # file uploads

# AI floor-plan analysis (optional — app falls back to a template layout if missing)
opencv-python-headless>=4.9
numpy>=1.26

# Notes:
#  - Auth uses the Python standard library (pbkdf2 + HMAC tokens) — no extra crypto deps.
#  - 3D generation writes .glb with the standard library — no Blender/trimesh needed to start.
#  - Video render (Phase 6) shells out to FFmpeg if it is installed; otherwise it is skipped.
pypdfium2
ezdxf
svglib
reportlab
pymupdf
ifcopenshell
scipy
scikit-image
